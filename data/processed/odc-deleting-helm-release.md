# Deleting a Helm release

.Procedure
1. In the *Topology* view, right-click the Helm release and select *Delete Helm Release*.
1. In the confirmation prompt, enter the name of the chart and click *Delete*.